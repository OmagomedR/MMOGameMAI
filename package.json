{
  "name": "mmogamemai",
  "version": "1.0.0",
  "scripts": {
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up",
    "docker:dev": "docker-compose -f docker-compose.override.yml up",
    "docker:down": "docker-compose down"
  }
}